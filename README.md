Uses Yoneda lemma and existentials to fully encode some GADTs into TypeScript

```sh
npx tsx examples/[example].ts
```

I recommend beginning with `examples/expr.ts`, which is more thoroughly commented.